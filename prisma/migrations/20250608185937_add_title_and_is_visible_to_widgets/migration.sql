BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Widgets] ADD [IsVisible] BIT CONSTRAINT [Widgets_IsVisible_df] DEFAULT 1,
[Title] NCHAR(30);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
